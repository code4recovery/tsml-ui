{
  "compilerOptions": {
    "jsx": "react-jsx", // can remove if we shift off of emotion
    "jsxImportSource": "@emotion/react", // can remove if we shift off of emotion
  },
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
}
