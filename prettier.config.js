
/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
  arrowParens: 'avoid',
  quoteProps: 'preserve',
  singleQuote: true,
  trailingComma: 'es5',
  tabWidth: 2,
  useTabs: false
};

export default config;
